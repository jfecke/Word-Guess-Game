document.onkeyup= function(event) {hangman.keyPress(event)};

var hangman = {
    keyPress: function(event) {
    var key = event.key;
    switch(key) {
      case 'd':

      case 'w':

      case 'g':

        break;
      case 'h':

        break;
    }
  }
} 

